# Dependent Catalog Mode

Temporary integration fixture for automatic required-dependency installation. It requires
`temp.catalog-difficulty >=1.0.0` and adds the visible **Dependency Test** game mode.
