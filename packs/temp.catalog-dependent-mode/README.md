# Dependent Catalog Mode

Temporary integration fixture for automatic required-dependency installation. It requires
`temp.catalog-support >=1.0.0` and adds the visible **Dependency Test** game mode. Version 1.2.0
is also the update fixture for local-change detection and preservation.
